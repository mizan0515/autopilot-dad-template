{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "autopilot config",
  "description": "Project-level autopilot configuration. Written by apply.{ps1,sh} on template install; hand-edit to change.",
  "type": "object",
  "required": ["project_name", "operator_language"],
  "properties": {
    "project_name": {
      "type": "string",
      "description": "Human-readable project name."
    },
    "project_description": {
      "type": "string",
      "description": "One-paragraph description used by the agent for context."
    },
    "product_directive": {
      "type": "string",
      "description": "The north-star product directive. Embedded into PROMPT.md IMMUTABLE block."
    },
    "operator_language": {
      "type": "string",
      "description": "IETF BCP-47 language tag (en, ko, ja, zh-CN, es, fr, de, ...). Operator-facing output uses this.",
      "default": "en"
    },
    "template_version": {
      "type": "string",
      "description": "Template revision this project was bootstrapped from."
    },
    "autopilot_ai": {
      "type": "string",
      "enum": ["claude", "codex", "custom"],
      "description": "Which AI the runner pipes PROMPT.md to.",
      "default": "claude"
    },
    "next_delay_default": {
      "type": "integer",
      "minimum": 60,
      "maximum": 3600,
      "default": 900,
      "description": "Default wait seconds between iters when NEXT_DELAY is unset."
    },
    "search_roots": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Project tree roots the agent prefers when grepping/globbing. Auto-detected at apply time; hand-edit to add project-specific paths."
    },
    "sensitive_delete_paths": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Path prefixes whose deletion requires a 'cleanup-operator-approved: yes' commit trailer. Enforced by .autopilot/hooks/commit-msg-protect.sh. Auto-populated at apply time with detected source/test/docs trees; always includes 'Document/' for the DAD dialogue history.",
      "default": ["Document/"]
    }
  }
}
