interface:
  display_name: "CardGame DAD v2 Repeat Workflow"
  short_description: "Run the next turn of the active CardGame DAD v2 session"
  default_prompt: "Use $cardgame-repeat-workflow to execute the next turn of the active CardGame DAD v2 session and emit the next handoff prompt."

policy:
  allow_implicit_invocation: false
