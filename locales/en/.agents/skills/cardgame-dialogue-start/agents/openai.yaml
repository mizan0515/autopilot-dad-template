interface:
  display_name: "CardGame DAD v2 Dialogue Start"
  short_description: "Start a CardGame Claude Code session with Codex as turn 1"
  default_prompt: "Use $cardgame-dialogue-start to start a CardGame DAD v2 session and produce Turn 1 plus the Claude Code handoff prompt."

policy:
  allow_implicit_invocation: false
